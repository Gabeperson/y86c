let x: *int = main as *int + 1;
let y: int;
struct Foo {
    foo: *int,
}
fn main() {
    //let x: int;
    //// addr of taken x with uninitialized issue?
    //assert(&x);

    let x: int;
    assert(~x);
}
 
