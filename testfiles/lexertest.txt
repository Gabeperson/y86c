int main() {
    let a: int = 42;
    let b: int = 0b110101;
    let c: int = 0xaffc015;
    a += b;
    c--;
    --c;
}
// This is technically invalid syntax
// structs are typescript-syntax
struct x {
    int x;
    struct somethingelse y;
    struct _something z;
    int*********** som;
};

+= -= == = === != ! | || & && &&& () (()) [[]] [] {{{{{{ }}}}}}}} ....,,,;;;:::
ljaksdflkj
+ - * ** / / 
<= >= < > << >> ^ ^^ ^= |= &= /= ? : . % %= *= /= 
++ -- --- +++ &= |= ^= %= ->->->


very_long_idents_that_are_very_very_very_very_long_and_impractical_but_whatever;00001 structident
0
